function fig = figS03d_fallback_state_machine(cfg)
%FIGS03D_FALLBACK_STATE_MACHINE Evidence-aware fallback logic.
if nargin < 1 || isempty(cfg), cfg = setup_portable_paths(); end
fig = new_figure(cfg, [80 80 1000 650]);
ax = axes(fig, 'Position', [0.03 0.10 0.94 0.82]); hold(ax, 'on');
axis(ax, [0 1 0 1]); axis(ax, 'off');
draw_flow_box(ax, [0.04 0.68 0.26 0.17], ...
    ['Single-image prototype' newline 'coverage + stability pass'], ...
    [0.90 0.97 0.91], cfg.colors.green);
draw_flow_box(ax, [0.39 0.68 0.26 0.17], ...
    ['Training conditional prototype' newline 'external reference'], ...
    [0.90 0.95 1.00], cfg.colors.blue);
draw_flow_box(ax, [0.70 0.68 0.26 0.17], ...
    ['Conservative interval' newline 'no precise defect type'], ...
    [0.98 0.94 0.87], cfg.colors.orange);
draw_flow_box(ax, [0.39 0.23 0.26 0.17], ...
    ['Human review' newline 'refuse automatic assessment'], ...
    [0.99 0.91 0.91], cfg.colors.red, 'FontWeight', 'bold');
draw_arrow(ax, [0.305 0.765], [0.385 0.765], cfg.colors.dark, 1.4);
draw_arrow(ax, [0.655 0.765], [0.695 0.765], cfg.colors.dark, 1.4);
draw_arrow(ax, [0.83 0.665], [0.58 0.405], cfg.colors.red, 1.4);
text(ax, 0.345, 0.83, 'mask > 60% / unstable views', ...
    'HorizontalAlignment', 'center', 'FontSize', 9, 'Color', cfg.colors.red);
text(ax, 0.675, 0.83, 'domain shift', ...
    'HorizontalAlignment', 'center', 'FontSize', 9, 'Color', cfg.colors.red);
text(ax, 0.73, 0.53, 'evidence remains insufficient', 'Rotation', -34, ...
    'HorizontalAlignment', 'center', 'FontSize', 9, 'Color', cfg.colors.red);
title(ax, '(d) Explicit fallback state machine');
add_figure_note(fig, 'simulation');
end
